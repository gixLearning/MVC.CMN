@model MVC.CMN.Models.Threaditem

<tr>
    <td>


            <div class="col-12 no-padding board-header">

                <div class="cell-header col-12">
                    <p class="no-margin">
                        @Html.ActionLink(Model.Title, "ShowThread", "Home", new { id = Model.Id }, null)
                    </p>
                </div>

                <div class="d-flex cell-content no-margin bottom-padding col-12 flex-wrap">
                    <div class="col-10 no-padding">
                        <p class="no-margin">
                            @Model.Posts.FirstOrDefault().Content                       @*See if this can be limited to the first line of content only!*@
                        </p>
                    </div>

                    <div class="col-2 no-padding">
                        @Model.Author
                        <br />
                        Add user info here


                    </div>
                </div>

                <div class="cell-footer col-12 d-flex flex-wrap">
                    <div class="col-10 no-padding">
                        <p class="small no-margin">
                            Written by @Model.Author on InsertDateHere
                        </p>
                    </div>


                    <div class="col-2 no-padding">

                        <p class="text-right small no-margin">
                            @Html.ActionLink("Edit thread", "EditThread", "Home", new { id = Model.Id }, null)

                            @Html.ActionLink("Delete thread", "DeleteThread", "Home", new { id = Model.Id }, null)

                        </p>
                    </div>

                </div>
            </div>

       





        @*<div class="col-12 no-padding standard-cell">

            <div class="col-12 no-padding  d-flex flex-wrap">

                <div class="cell-content bottom-padding col-8">

                    <div class="no-padding  text-danger col-12">
                        @Html.ActionLink(Model.Title, "ShowThread", "Home", new { id = Model.Id }, null)

                    </div>

                    <div class="col-12  no-padding">
                        <p class="no-margin">
                            @Model.Posts.FirstOrDefault().Content                       See if this can be limited to the first line of content only!
                        </p>
                    </div>

                </div>

                <div class="cell-content small col-1 d-flex flex-wrap flex-row">
                    <p class="my-auto">45</p>
                </div>
                <div class="cell-content small col-1 d-flex flex-wrap flex-row">
                    <p class="my-auto">132</p>
                </div>
                <div class="cell-content small col-2 d-flex flex-wrap flex-row">
                    <p class="my-auto">
                        Gorgoflex the Untamed
                        <br />
                        17/6/18, 20:22
                    </p>

                </div>

            </div>

            <div class="cell-footer col-12 d-flex flex-wrap">

                <div class="col-10 no-padding">
                    <p class="small no-margin">
                        Written by @Model.Author on InsertDateHere
                    </p>
                </div>


                <div class="col-2 no-padding">

                    <p class="text-right small no-margin">
                        @Html.ActionLink("Edit thread", "EditThread", "Home", new { id = Model.Id }, null)

                        @Html.ActionLink("Delete thread", "DeleteThread", "Home", new { id = Model.Id }, null)

                    </p>
                </div>
            </div>


        </div>*@





    </td>
</tr>
