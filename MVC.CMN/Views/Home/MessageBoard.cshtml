@model  List<MVC.CMN.Models.Boarditem>

@{
    ViewBag.Title = "MessageBoard";
}

<div>
    <div class="panel rounded border border-info col-4 offset-4 panel-transparent p-2 m-2">
        <h2 class="text-center">MessageBoard</h2>
    </div>
</div>

<div class="d-flex">





    <table class="col-12 table-hover">

        @{
            foreach (var board in Model)
            {
                @Html.Partial("_BoardCell", board)

            }
        }

    </table>
</div>